apiVersion: v1
kind: Service
metadata:
  name: testing-ops
  namespace: dev
spec:
  selector:
    app: testing-ops
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
  type: ClusterIP
