apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: sample-app-ingress
  namespace: prod
  annotations:
    kubernetes.io/ingress.class: traefik
spec:
  rules:
    - host: sample-app.prod.157.180.69.112.nip.io
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: sample-app
                port:
                  number: 80
