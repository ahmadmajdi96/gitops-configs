apiVersion: apps/v1
kind: Deployment
metadata:
  name: testing_ops
  namespace: prod
spec:
  replicas: 3
  selector:
    matchLabels:
      app: testing_ops
  template:
    metadata:
      labels:
        app: testing_ops
    spec:
      containers:
      - name: testing_ops
        image: ghcr.io/ahmadmajdi96/testing_ops:latest   # update with a stable tag
        ports:
        - containerPort: 8080
        readinessProbe:
          httpGet:
            path: /
            port: 8080
        livenessProbe:
          httpGet:
            path: /
            port: 8080
